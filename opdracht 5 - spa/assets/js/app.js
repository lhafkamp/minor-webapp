'use strict';

const greatSuccesModel = (() => {
	const derp = 'hi';

	const app = {
		init() {
			console.log(derp);
		},
	}

	const routes = {
		init() {

		},
	}

	const sections = {
		init() {

		},
	}
	app.init();
})();